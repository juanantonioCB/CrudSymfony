{{ form_start(form) }}
    {{ form_widget(form) }}
{% if empleado.Imagen %}
    <img src="{{ asset('uploads/brochures/' ~ empleado.Imagen) }}"></img>
{% endif %}
    <button class="btn btn-success">{{ button_label|default('Guardar') }}</button>
{{ form_end(form) }}
